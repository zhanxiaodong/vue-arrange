/* 超小屏幕（手机，小于 768px） */
/* 没有任何媒体查询相关的代码，默认（移动设备优先） */

/* 小屏幕（平板，大于等于 768px） */
$screen-sm-min = 768px

/* 中等屏幕（桌面显示器，大于等于 992px） */
$screen-md-min = 992px

/* 大屏幕（大桌面显示器，大于等于 1200px） */
$screen-lg-min = 1200px
